# Third-party notices (pointers)

This file is intentionally a *pointer list* and not a legal opinion. Before redistribution, verify all license obligations.

## Bundled sources

- `external/libzt/` – upstream `libzt` sources + license: `external/libzt/LICENSE.txt`

## NuGet dependencies

Library:

- `Microsoft.Extensions.Logging.Abstractions`
- `System.Memory.Data`

Tests:

- `xunit`
- `xunit.runner.visualstudio`
- `Microsoft.NET.Test.Sdk`
- `coverlet.collector`

